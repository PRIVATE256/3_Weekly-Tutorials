# Create a program that stores an integer, a floating-point number, a string, and a boolean variable, and then outputs them.

zahl = int(10)
kommazahl = float(10.5)
text = str("Hello, World!")
wahrheitswert = bool(True)

print("zahl variable contains ", zahl, " and is of datatype ", type(zahl))
print("kommazahl variable contains ", kommazahl, " and is of datatype ", type(kommazahl))
print("zahl variable contains ", text, " and is of datatype ", type(text))
print("zahl variable contains ", wahrheitswert, " and is of datatype ", type(wahrheitswert))